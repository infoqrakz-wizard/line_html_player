/**
 * Константы для компонента Timeline
 */

/**
 * Интервалы масштабирования в миллисекундах
 */
export const INTERVALS = [
    5 * 60 * 1000, // 5 минут
    10 * 60 * 1000, // 10 минут
    15 * 60 * 1000, // 15 минут
    30 * 60 * 1000, // 30 минут
    60 * 60 * 1000, // 1 час
    4 * 60 * 60 * 1000, // 4 часа
    6 * 60 * 60 * 1000, // 6 часов
    12 * 60 * 60 * 1000, // 12 часов
    24 * 60 * 60 * 1000 // 1 день
];

/**
 * Длина единицы времени для каждого интервала (в секундах)
 * Используется для агрегации посекундных данных при отрисовке
 * Все данные всегда загружаются с unit_len=1, эти значения используются только для отрисовки
 */
export const UNIT_LENGTHS = [
    1, // 5 минут - 1 сек на фрагмент (для точности)
    2, // 10 минут - 2 сек на фрагмент
    3, // 15 минут - 3 сек на фрагмент
    5, // 30 минут - 5 сек на фрагмент
    10, // 1 час - 10 сек на фрагмент
    30, // 4 часа - 30 сек на фрагмент
    60, // 6 часов - 1 мин на фрагмент
    120, // 12 часов - 2 мин на фрагмент
    300 // 24 часов - 5 мин на фрагмент (вместо 24 минут для лучшей детализации)
];

/**
 * Количество экранов для буферизации в каждую сторону
 */
export const BUFFER_SCREENS = 1;

/**
 * Порог для накопления дельты колесика мыши
 */
export const WHEEL_DELTA_THRESHOLD = 50;

/**
 * Минимальное расстояние для определения вертикального свайпа
 */
export const VERTICAL_SWIPE_THRESHOLD = 30;

/**
 * Минимальное расстояние для определения горизонтального свайпа
 */
export const HORIZONTAL_SWIPE_THRESHOLD = 20;

/**
 * Минимальная дистанция для изменения масштаба при вертикальном свайпе
 */
export const ZOOM_SWIPE_DISTANCE = 75;

/**
 * Минимальная дистанция для переключения камеры при горизонтальном свайпе (в процентах от ширины экрана)
 */
export const CAMERA_SWIPE_THRESHOLD_PERCENT = 30;

/**
 * Минимальная дистанция для определения горизонтального свайпа по плееру
 */
export const PLAYER_HORIZONTAL_SWIPE_THRESHOLD = 30;

/**
 * Константы для позиций меток на временной шкале
 */
export const TIMELINE_POSITIONS = {
    DATE_LABEL_TOP: 2, // Метка даты в самом верху
    DATE_LABEL_HEIGHT: 18, // Высота метки даты
    HOUR_LABEL_TOP: 25, // Метка часа ниже метки даты
    HOUR_LABEL_HEIGHT: 16, // Высота метки часа
    MINUTE_LABEL_TOP: 0, // Будет вычислено динамически на основе высоты контейнера
    MARKER_SPACING: 40, // Фиксированное расстояние между метками в пикселях

    // Константы для высоты маркеров (от самого высокого к самому низкому)
    DAY_MARKER_HEIGHT: 45, // Высота маркера дня (самые высокие)
    SIX_HOUR_MARKER_HEIGHT: 40, // Высота маркера 6 часов
    HOUR_MARKER_HEIGHT: 35, // Высота маркера часа
    HALF_HOUR_MARKER_HEIGHT: 30, // Высота маркера 30 минут
    QUARTER_HOUR_MARKER_HEIGHT: 25, // Высота маркера 15 минут
    FIVE_MIN_MARKER_HEIGHT: 20, // Высота маркера 5 минут
    MINUTE_MARKER_HEIGHT: 15 // Высота маркера минуты (самые короткие)
};
